{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Rebrew window is allowed to do. Deliberately small: no filesystem, no shell, no http, no dialogs. Everything that touches disk happens in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-set-always-on-top"
  ]
}
